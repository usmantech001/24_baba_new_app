import 'package:flutter/material.dart';

class DriverLiscenseScreen extends StatelessWidget {
  const DriverLiscenseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}